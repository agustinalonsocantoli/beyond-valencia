// Components
import { Navbar } from '@/components/Navbar';

export const Experiences = () => {
    return(
        <div>
            <Navbar title={"Experiences"} />

            Experiences
        </div>
    );
}